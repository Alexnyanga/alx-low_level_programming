#include <stdio.h>
/**
 * main - printing lowercase numbers
 * Return:m(0)
 */

int main(void)

{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low &&!q, &&!e);

	for (low = 'A'; low <= 'Z'; low++)
		putchar(low &&!Q, &&!E);

	return (0);

}
